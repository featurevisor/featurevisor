/** @type {import('prettier').Config} */
const config = {
  printWidth: 100,
  singleQuote: false,
  trailingComma: "all",
  tabWidth: 2,
  semi: true,
};

// export default config
module.exports = config;
