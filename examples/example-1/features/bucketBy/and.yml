description: Buckets users by combining userId and deviceId together
tags:
  - all

bucketBy:
  - userId
  - deviceId

rules:
  staging:
    - key: "1"
      segments: "*"
      percentage: 100
  production:
    - key: "1"
      segments: "*"
      percentage: 50
