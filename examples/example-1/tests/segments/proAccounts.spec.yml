segment: proAccounts
assertions:
  - context:
      account:
        plan: pro
        metadata:
          locale: nl-NL
    expectedToMatch: true

  - context:
      account:
        plan: free
        metadata:
          locale: nl-NL
    expectedToMatch: false
