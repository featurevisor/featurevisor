{
  "name": "featurevisor-example-project",
  "version": "0.0.0",
  "private": true,
  "description": "Example Featurevisor project — copy this directory, npm install, then npx featurevisor lint && npx featurevisor test",
  "scripts": {
    "lint": "featurevisor lint",
    "test": "featurevisor test",
    "build": "featurevisor build"
  },
  "devDependencies": {
    "@featurevisor/cli": "*"
  }
}
