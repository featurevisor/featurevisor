description: <Human-readable description>
type: string
# Other supported types: boolean, integer, double, date, array, object
# Use `oneOf` at top level instead of `type` for union-like attributes.

# Optional schema-ish properties for stricter linting / code generation:
# enum: [free, pro, enterprise]
# pattern: '^[a-z]{2}$'
# minLength: 2
# maxLength: 2
